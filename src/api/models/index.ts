/**
 * Request/response shapes for the API under test. Keeping them here (rather
 * than inline in clients) makes them reusable by data factories and specs.
 *
 * Example:
 *   export interface User { id: string; email: string; role: 'admin' | 'user' }
 *   export interface LoginResponse { token: string; expiresIn: number }
 */
export {};

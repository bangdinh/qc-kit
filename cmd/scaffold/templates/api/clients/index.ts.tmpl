/**
 * Điểm gom client của tầng API.
 *
 * Thêm tài nguyên mới: tạo `<Ten>Client.ts` cạnh file này rồi export thêm một dòng.
 * Spec luôn `createClient(TenClient)` — không tự `new`, để fixture cấp sẵn request
 * context và token.
 */
export * from './ExampleClient';

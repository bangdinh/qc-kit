/**
 * Điểm gom kiểu của tầng API.
 *
 * Spec import `../../src/api/models`, nên thêm một tài nguyên mới chỉ là thêm một dòng ở
 * đây — không spec nào phải sửa import.
 */
export * from './example.model';

/**
 * Điểm gom verification của tầng API.
 *
 * Một tài nguyên một class. Chỉ đưa vào đây thứ đúng với MỌI test của tài nguyên đó;
 * assert riêng của một kịch bản thì ở lại trong spec.
 */
export * from './ExampleVerification';

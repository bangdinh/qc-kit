/**
 * Phần lõi do DỰ ÁN sở hữu — luồng đăng nhập và cache session.
 *
 * Mọi năng lực cắt ngang khác (BasePage, BaseComponent, logger, step) vẫn đến từ
 * `qc-kit/core`. Chỉ phần đăng nhập ở đây, vì chỉ dự án này biết màn đăng nhập trông ra
 * sao, session cache ở đâu, và cookie được dùng lại bao lâu.
 */
export * from './auth';
export * from './paths';
export * from './session';
